# Thronos Driver Service

Micro‑service for Thronos Driver telemetry, trips, and safety scoring.
Designed to run as a standalone service (e.g. on Railway) with an attached 50GB volume
for the database file.

See `.env.example` for configuration and `app/main.py` for FastAPI entrypoint.
